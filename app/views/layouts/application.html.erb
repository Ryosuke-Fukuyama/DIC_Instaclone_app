<!DOCTYPE html>
<html>
  <head>
    <title>SessionLogin</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header class="header clearfix">
      <nav>
        <ul class="top_menu clearfix">
          <% if logged_in? %>
            <li class="top_left"><i>&nbsp;Insta</i></li>
            <li class="top_left"><%= link_to "投稿一覧", pictures_path %></li>
            <li class="top_right"><%= link_to "プロフィール", user_path(current_user.id) %></li>
            <li class="top_right"><%= link_to "ログアウト", session_path(current_user.id), method: :delete %></li>
          <% else %>
            <li><%= link_to "アカウント作成", new_user_path %></li>
            <li><%= link_to "ログイン", new_session_path %></li>
          <% end %>
        </ul>
      </nav>
    </header>
      <% flash.each do |key, value| %>
        <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
      <%= yield %>
      <footer></footer>
  </body>
</html>
